import ComprehensionData from './CompaniesData';

const INITIAL_STATE = {
    comprehensionData: ComprehensionData
};

const comprehensionReducer = ( state = INITIAL_STATE, action ) => {
    switch (action.type){
        default: 
            return state
    }
}

export default comprehensionReducer;